{
  "name": "test-vite",
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run clean && vite serve src",
    "debug": "npx --node-arg=--inspect vite serve src --debug",
    "clean": "rm -rf node_modules/.vite_opt_cache",
    "build": "vite build src --outDir dist "
  },
  "dependencies": {
    "test-package": "link:./test-package",
    "vite-plugin-react": "^2.0.3"
  },
  "devDependencies": {
    "vite": "^0.20.2"
  }
}
